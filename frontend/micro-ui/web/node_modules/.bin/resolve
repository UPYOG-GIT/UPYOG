#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../micro-ui-internals/packages/libraries/node_modules/@babel/core/node_modules/resolve/bin/resolve" "$@"
  ret=$?
else 
  node  "$basedir/../../micro-ui-internals/packages/libraries/node_modules/@babel/core/node_modules/resolve/bin/resolve" "$@"
  ret=$?
fi
exit $ret
